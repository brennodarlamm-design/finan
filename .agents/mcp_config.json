{
  "mcpServers": {
    "upstash": {
      "url": "https://mcp.upstash.com/mcp",
      "headers": {
        "Authorization": "Bearer ${UPSTASH_API_KEY}"
      },
      "description": "Upstash Remote MCP Server para gerenciamento e consulta de recursos Upstash (Redis, QStash, Vector, Workflow)"
    },
    "context7": {
      "url": "https://mcp.context7.com/mcp",
      "serverUrl": "https://mcp.context7.com/mcp",
      "headers": {
        "CONTEXT7_API_KEY": "${CONTEXT7_API_KEY}",
        "Authorization": "Bearer ${CONTEXT7_API_KEY}"
      },
      "description": "Context7 Remote MCP Server para consulta em tempo real de documentações e bibliotecas"
    }
  }
}
